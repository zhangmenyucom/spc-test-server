server:
  port: 8088
spring:
    cloud:
       config:
         server:
            git:
               uri: https://github.com/zhangmenyucom/config-center
               searchPaths: '{application}'
               force-pull: true
logging:
  level: debug
